// external imports
import React from 'react'
import PropTypes from 'prop-types'
import { mount } from 'enzyme'
// local imports
import Node, {NoChildren, WithChildren, Text} from '.'

describe('Source', function() {
    test('Mounting the source of a component with no children instantiates a NoChildren component', () => {

    })
})
