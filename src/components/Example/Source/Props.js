// external imports
import React from 'react'

const Props = ({children: node}) => {
    // if there is no node or props
    if (!node || !node.props) {
        // don't render anything
        return null
    }

    return null
}

export default Props
