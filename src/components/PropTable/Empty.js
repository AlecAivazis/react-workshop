// external imports
import React from 'react'

const EmptyPropTable = ({...unused}) => (
    <div {...unused}>
        no prop types found for this component
    </div>
)

export default EmptyPropTable
