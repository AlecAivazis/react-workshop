import { dims } from 'client/styles'

export default {
  container: {
    marginBottom: 5 * dims.base,
  },
  examples: {
    display: 'flex',
    flexDirection: 'column',
  },
  title: {
    marginBottom: dims.base,
    fontSize: 18,
  }
}
