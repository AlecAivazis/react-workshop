// external imports
import React from 'react'
// local imports
import styles from './styles'

const PropTable = ({style}) => (
    <div style={{...styles.container, ...style}}>
        prop types for this component
    </div>
)

export default PropTable
